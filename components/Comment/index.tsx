/**
 * Kept at its original path so existing imports keep working; the threaded
 * implementation lives with the rest of the feed components.
 */
export { default } from "@/components/feed/Comments";
export type { CommentNode } from "@/components/feed/Comments";
