# WebSocket Signalling Server
NEXT_PUBLIC_BACKEND=wss://signalling-server-ztzs.onrender.com/

# Database (Neon / PostgreSQL)
DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require&channel_binding=require

# NextAuth
NEXTAUTH_URL=https://<your-app>.vercel.app
NEXTAUTH_SECRET=<some-long-random-secret>
JWT_SECRET=<jwt-secret>

# Google OAuth
GOOGLE_CLIENT_ID=<your-google-client-id>
GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# Cloudinary
CLOUDINARY_API_KEY=<your-cloudinary-api-key>
CLOUDINARY_API_SECRET=<your-cloudinary-api-secret>
CLOUDINARY_CLOUD_NAME=<your-cloudinary-cloud-name>
CLOUDINARY_URL=cloudinary://<api-key>:<api-secret>@<cloud-name>

# Email
EMAIL_FROM=YourApp <your-email@gmail.com>
EMAIL_SERVER=smtp.gmail.com
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=<your-email@gmail.com>
EMAIL_SERVER_PASSWORD=<your-app-password>

# The canonical origin. Open Graph needs absolute URLs, and a link preview
# resolved against the wrong origin renders as a broken image everywhere it is
# shared. Falls back to VERCEL_URL, then localhost.
NEXT_PUBLIC_SITE_URL=https://<your-app>.vercel.app

# Shared secret for the scheduled AI post. The endpoint refuses every request
# when this is unset, so leaving it blank disables the bot rather than leaving
# it open to the world.
# Call it with: Authorization: Bearer <secret>
CRON_SECRET=<some-long-random-secret>

# Misc
HASH=<hash-value>
NEXT_PUBLIC_CLOUDINARY_API_KEY=<your-cloudinary-api-key>
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=<your-cloudinary-cloud-name>
PORT=10000
